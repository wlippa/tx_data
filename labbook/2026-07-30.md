# 2026-07-30

## 13:11 UTC — Repo scaffolded (commit `4cef840`)

Initial layout at `/home/homer/.openclaw/workspace/projects/tx_data`.

Key choices:
- DuckDB over Parquet, YAML catalog.
- Path abstraction via `TX_DATA_ROOT` env var.
- `nemo_mock/` mirrors `/Nemo`, checked into git.
- Package layout under `src/tx_data/`.

Formal decisions in `labbook/DECISIONS.md` entries `-001`, `-002`.

## 13:34 UTC — Agent + lab book scaffold

Added `agent/SYSTEM.md` plus detail prompts under `agent/prompts/`:
- `conventions.md` — Python + naming + commit style
- `data-model.md` — YAML catalog format
- `etl-pattern.md` — standard ETL script shape + skeleton
- `privacy.md` — governance rules for real vs mock data + LLM boundary
- `review.md` — pre-commit checklist

Lab book scaffold:
- This file (`2026-07-30.md`) — raw daily log
- `STATE.md` — curated current state
- `DECISIONS.md` — architecture decision records (with entries `-001` through `-004`)

Formal decisions in `labbook/DECISIONS.md` entries `-003`, `-004`.

## Open at end of session

- Awaiting first table description from Odysseus: `/Nemo` path, format, columns, semantics, primary key, relationships.
- Loop plan: mock file + conversion script + catalog entry + test in one commit per table.
